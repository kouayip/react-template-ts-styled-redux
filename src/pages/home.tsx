import styled from '@emotion/styled'
import React, { useEffect } from 'react'
import {  Container, Page } from '../components'

const ProfileInner = styled(Container)`
  margin-top: 1rem;
`

const HomePage: React.FC = () => (
<Container>
<h1>Welcome</h1>
  </Container>)

export default HomePage
